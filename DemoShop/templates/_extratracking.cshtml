@if (PageData["util"] != null)
{
    @Html.Raw(PageData["util"].CreateJavaScriptEventCode(
        PageData["trackingEvents"], 
        HttpContext.Current.Session.SessionID,
        PageData["ffParameters"].Channel
    ));
}

function onSuggestCompleted(displayed, suggestResult) {
    trackEvent('display', suggestResult[0].refKey, '@HttpContext.Current.Session.SessionID', '@PageData["ffParameters"].Channel', {});
}